{
  "name": "rog-pool-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
  },
  "workspaces": ["frontend"],
  "engines": {
    "node": ">=18.0.0"
  }
}